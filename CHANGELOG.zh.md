# 更新日志

<p align="center">
  <a href="CHANGELOG.md">🇺🇸 🇬🇧 English</a> ·
  <a href="CHANGELOG.ru.md">🇷🇺 Русский</a> ·
  <a href="CHANGELOG.uk.md">🇺🇦 Українська</a> ·
  <a href="CHANGELOG.be.md">🇧🇾 Беларуская</a> ·
  <a href="CHANGELOG.fr.md">🇫🇷 Français</a> ·
  <a href="CHANGELOG.es.md">🇪🇸 Español</a> ·
  <a href="CHANGELOG.hi.md">🇮🇳 हिन्दी</a> ·
  <a href="CHANGELOG.zh.md">🇨🇳 中文</a> ·
  <a href="CHANGELOG.ja.md">🇯🇵 日本語</a> ·
  <a href="CHANGELOG.ar.md">🇸🇦 العربية</a>
</p>

这里记录 **YouTube Harvester** 的所有重要变化。

## [1.2.0-beta] - 2026-08-29

### 新增

- 可在概览 URL 输入框、快速下载、剪贴板监控、手动队列和档案中选择下载
  YouTube、Rutube 与 VK 的单个视频。
- 档案记录来源与媒体 ID，提供规范来源链接，并为 VK 和 Rutube 显示单色
  `Ⓥ` 与 `Ⓡ` 标志。
- 主题选择器旁新增关闭按钮；频道列限制为 160 px，媒体 ID 列限制为 155 px。
- 新增支持网址、档案兼容与迁移、文件移动和安全预览图下载的自动化测试。
- 新增完整的 Windows 依赖锁定文件和 Dependabot 配置。

### 变更

- 应用及发布工具将此版本标记为 `1.2.0-beta` 预发布版；Debian 使用
  `1.2.0~beta1` 以保持正确的版本排序。
- 固定组件版本：`yt-dlp 2026.08.19`、`yt-dlp-ejs 0.8.0`、Deno `2.9.6`、
  Windows FFmpeg/FFprobe `9.0.1`、PyQt5 `5.15.11`、pynput `1.8.2`、
  PyInstaller `6.22.2` 和 Pillow `12.3.0`。
- Windows 在线与离线构建会核对组件的准确版本和校验和，运行 `pip check`，
  并使用完整的传递依赖锁定文件。
- GitHub Actions 固定到经过审核的提交；Windows 离线文档已更新为固定工具和
  Inno Setup 7.1。
- Linux 软件包元数据改为描述带 YouTube、Rutube 与 VK 手动队列的视频下载器。
- 新增的网址和档案来源文字已翻译到所有界面语言。

### 修复

- 应用更新比较现在会正确排列 alpha、beta、release candidate 和稳定版，
  不会把较新的 beta 降级到较旧的稳定版。
- 队列、重复检查、画质与轨道变体、档案删除、报告及迁移都使用“来源 + ID”，
  因而不同服务中的相同 ID 不会冲突。
- 旧档案记录仍作为 YouTube 条目兼容；迁移不会猜测含糊 ID 的来源，最终文件名
  也会移除内部来源标记。
- YouTube 专用的元数据与 HTTP 403 回退不再用于 Rutube 或 VK；缺少
  `yt-dlp` 输出流时也能正常处理而不崩溃。
- 频道回调会绑定当前频道和媒体类型；设置命令的输出明确按 UTF-8 解码。
- Windows 构建会忽略 `PATH` 中版本不兼容的 FFmpeg 或 Deno，并在在线模式
  下载经过校验的固定版本。

### 安全

- 预览图仅通过 HTTP(S) 原子下载，拒绝 URL 凭据，验证重定向，并设有超时、
  12 MiB 上限及未完成临时文件清理。
- FFmpeg 与 Deno 软件包在使用前通过 SHA-256 校验；变体和文件名摘要也由
  SHA-1 改为 SHA-256。
- 即使在提交前构建，发布源码压缩包也会包含依赖锁、测试和其他必需的新文件。

## [1.1.3] - 2026-08-20

### 新增

- 内置应用更新器可从 GitHub 官方 Release 下载匹配的安装程序、便携压缩包、
  Linux 软件包或源码压缩包。
- 更新支持断点续传，打开前会核对公开的 SHA-256 和 GitHub 资源摘要。
- 完整的白俄罗斯语界面、使用规则、README、更新日志、桌面元数据和截图。
- 为 FriendsHub 等桌面集成提供安全的单实例命令和下载完成系统通知。

### 变更

- 内置 `yt-dlp` 更新到 `2026.08.19`，Windows 构建依赖与 GitHub Actions
  同步更新到当前稳定版。
- 设置中的 `yt-dlp` 更新器会以原子方式安装经过校验的托管可执行文件。
- 遇到 HTTP 403 时会刷新 YouTube 媒体地址，并在最终报错前使用备用客户端重试。

### 修复

- 系统托盘、任务栏和组合显示模式会一致地应用于主窗口和快速下载窗口。
- 只有完成的文件到达最终目录后才写入档案，避免错误的成功记录。
- Linux 的锁、启动请求和完成事件使用用户私有目录，并拒绝不安全文件。

## [1.1.2] - 2026-08-01

### 新增

- 快速下载可选择多条音轨以及多条手动或自动字幕轨，并将它们嵌入同一个 MP4。
- 完整日语界面、日语使用规则、README、更新日志和界面截图。
- 音轨和字幕菜单优先显示原始或手动轨道，然后依次显示俄语、英语、乌克兰语及
  其他界面语言；其余语言在单独分组中按字母排序。

### 变更

- 档案将分辨率、音轨组合和字幕组合不同的文件视为同一视频的不同版本。
- 档案的画质列仅显示分辨率；悬停提示会逐行列出所选音轨和字幕轨。
- 合并后的 MP4 音频流会写入 ISO 639 语言元数据。
- 字幕选择器改用当前字体可正常显示的 `🔤` 图标。
- 字幕轨菜单会用分隔线区分手动/原始字幕与自动字幕。

### 修复

- 快速下载会使用额外的 YouTube 元数据客户端，查找默认客户端遗漏的其他配音。
- 单条字幕下载失败（包括 HTTP 429）时，`yt-dlp` 会移除该轨后重试；如果全部
  字幕均失败，视频和音频仍会正常下载。
- Windows 下延迟保存快速下载窗口的位置，拖动窗口时不再抖动。
- 修正多轨版本与旧单轨档案记录的比较，并限制 Windows 安全版本文件名的长度。
- 托盘中的“停止”操作在所有语言下都使用清晰可见的 `🛑` 图标。

## [1.1.1] - 2026-07-25

### 修复

- 每个已完成的频道视频现在会在下一个播放列表项目开始前，从临时目录移到
  下载目录。
- 软停止会保留并移动已经完成的视频。
- 即使隐藏游戏的胜利标志已经解锁，下载时仍会显示当前频道图片。
- 修复了一些界面和运行逻辑的小问题。

## [1.1.0] - 2026-07-25

### 新增

- 完整支持英语、俄语、乌克兰语、法语、西班牙语、印地语、中文和阿拉伯语；
  新安装默认使用英语。
- 每种语言都有独立 README、更新日志和界面截图。
- 系统、X11/Wayland、托盘、快捷键、剪贴板、工具、路径、缓存、写入权限和
  磁盘空间诊断。
- 检查当前和最新 `yt-dlp` 版本。
- 每个频道的付费内容状态，以及主动频道检查时可选的 members-only 搜索。
- 按普通视频、Shorts、直播和队列分类的每日下载报告。
- 日志新增“全部”“重要”和“错误”筛选。
- 概览页新增立即下载和快速下载操作。
- 使用本地频道缓存的可重复多语言截图生成器。

### 变更

- 应用和构建脚本版本更新为 `1.1.0`。
- 队列在频道检查前处理，并在全部频道完成后再次处理。
- Members-only 现在作为重要访问提示，而不是红色下载错误。
- 频道检查会显示并动态标识当前分区，同一按钮可以停止检查。
- 改进间距、复选框对比度、频道控制、限制行、概览工具栏和快速下载布局。
- Linux 只显示 Python 引擎；Bash 仅保留为已禁用历史代码。
- 显式 `YTD_CONFIG_DIR` 现在保存全部设置，隔离便携和测试实例。

### 修复

- Windows 控制台、日志、档案和子进程可以用 UTF-8 安全显示西里尔字符和表情。
- Telegram 或后处理失败不会删除已经成功保存的本地文件。
- Linux/X11 清理会忽略 `.yth-temp` 标记并安全删除已完成的临时文件。
- 手动下载后概览恢复占位图，下一次启动不会闪现旧频道图片。
- 开始下载后，剪贴板监控不会为同一 URL 反复打开快速下载。
- 快速下载保存窗口位置，更可靠地加载频道图片，正确容纳缩略图并使用圆形高亮。

## [1.0.0] - 2026-07-02

### 新增

- 第一个 Linux 和 Windows 稳定版本。
- 概览、频道、队列、计划任务、档案、日志、快速下载、剪贴板、快捷键、
  Telegram、主题和启动模式。
- `.deb`、源码、Setup EXE、MSI、便携 ZIP 和 SHA256。
- Windows 内置 `yt-dlp`、FFmpeg/FFprobe 和 Deno。
- 负责任使用规则和外部组件说明。

### 变更与修复

- Python 成为 Linux/Windows 共用引擎，共用工具移入 `yth_common.py`。
- 快速请求交给单一实例，不再产生重复托盘进程。
- Telegram 错误不再影响本地文件；退出码、临时清理和 PyInstaller 模块导入得到
  修复。

## [0.2.5-beta] - 2026-06-28

- 新增带剪贴板、元数据、分辨率、队列和 Telegram 的快速下载。
- 新增 Windows 原生快捷键、X11 `pynput` 和 Wayland 系统快捷键。
- Linux 切换为 Python，引导布局和预览更可靠。

## [0.2.4-beta] - 2026-06-25

- Windows 加入 `ffmpeg.exe`、`ffprobe.exe`、`deno.exe` 和自动发布流程。
- 修复 UTF-8、带空格路径、Windows 安全文件名和失败后的临时清理。

## [0.2.3-beta] - 2026-06-18

- 下载期间概览继续显示已检查频道数。
- Shorts 统一使用清晰的闪电图标。

## [0.2.2-beta] - 2026-06-13

- 新增使用规则、设置、实验性 Python 引擎、Windows 构建准备、发布打包和
  GitHub Actions。
- 改进紧凑限制、进度、下载阶段、分区暂停和空闲报告。
- 修复队列/档案重复和 Windows 表情显示。

## [0.2.0-beta.1] - 2026-06-12

- 第一个公开测试版，包含概览、频道图片、队列、计划任务、设置、Telegram、
  主题、日志和 Linux `.deb`。
- 修复托盘打开、日志刷新、表情和临时清理。

## [0.1.0] - 2026-06-11

- 第一个打包版本，包含托盘、频道、计划任务、队列、日志和 Telegram。
